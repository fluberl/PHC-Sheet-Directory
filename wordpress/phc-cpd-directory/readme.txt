# PHC CPD Directory — WordPress plugin

Shortcode:

```text
[phc_cpd_directory]
```

Outputs exactly:

```html
<div id="phc-cpd-directory"></div>
```

CSS and JavaScript load only on pages that render this shortcode.

Full installation, update, and rollback steps:

`docs/milestones/M13-wordpress-integration.md`
