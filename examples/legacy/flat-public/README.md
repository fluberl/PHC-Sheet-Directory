# Legacy flat PUBLIC fixtures

These `{ id, title }` JSON samples supported earlier generic-engine milestones.

They are **not** the PHC CPD PUBLIC Data Contract v1.0.

Current CPD PUBLIC fixture:

`examples/public/sample-public.json`
